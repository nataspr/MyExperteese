%h1 Main page
.center.jumbotron
  %h1= t('main.index.welcome')
  - if signed_in?
    = link_to t('main.index.proceed'), work_path, class: "btn btn-lg btn-primary"
  - else
    = link_to t('main.index.signup'), signup_path, class: "btn btn-lg btn-primary"